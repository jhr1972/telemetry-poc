apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "telemetry-poc.fullname" . }}-test-connection"
  labels:
    {{- include "telemetry-poc.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "telemetry-poc.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
